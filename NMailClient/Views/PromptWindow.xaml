<Window x:Class="NMailClient.Views.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i18n="clr-namespace:NMailClient.Services.I18n"
        Title="{i18n:T Title.Prompt}" Icon="pack://application:,,,/NMailClient;component/Assets/appicon.ico" SizeToContent="Height" Width="420"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False" FontFamily="Segoe UI" FontSize="13"
        Background="{DynamicResource Bg}">

  <StackPanel Margin="16">
    <TextBlock x:Name="LblPrompt" TextWrapping="Wrap" Margin="0,0,0,8" />
    <TextBox x:Name="TxtValue" Padding="6,4" />
    <TextBlock x:Name="LblError" Foreground="#C04F4F" TextWrapping="Wrap"
               Margin="0,6,0,0" Visibility="Collapsed" />

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
      <Button Content="OK" Width="88" Margin="0,0,6,0" IsDefault="True" Click="Ok_Click"
              Style="{StaticResource AccentButton}" />
      <Button Content="{i18n:T Common.Cancel}" Width="88" IsCancel="True" />
    </StackPanel>
  </StackPanel>
</Window>
