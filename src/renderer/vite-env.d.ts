/// <reference types="vite/client" />

/** App version, injected at build time from package.json. */
declare const __APP_VERSION__: string
/** Raw CHANGELOG.md contents, injected at build time. */
declare const __CHANGELOG__: string
